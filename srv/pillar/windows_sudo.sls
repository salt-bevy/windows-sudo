# Pillar data for the win11 test VM (see ../../sandbox and the salt-bevy
# Vagrantfile's "win11" machine, sourced from
# gusztavvargadr/windows-11-25h2-enterprise).

windows_sudo:
  repo_dir: C:\windows-sudo
